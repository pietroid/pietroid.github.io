import 'package:jaspr/dom.dart';

const primaryColorValue = 0x01589B;
const surfaceColorValue = 0xF7F2FB;

const primaryColor = Color.value(primaryColorValue);
const surfaceColor = Color.value(surfaceColorValue);
const loaderColor = Color.value(0xcccbf2);

const cardBorderRadius = 10.0;

const cardWidth = 360.0;
const cardHeight = 640.0;
